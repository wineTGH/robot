{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Robot: Deploy",
            "command": "${workspaceFolder}/scripts/deploy.sh",
            "type": "shell",
        },
        {
            "label": "Robot: Deploy and run debugger",
            "command": "'${workspaceFolder}/scripts/deploy.sh' && '${workspaceFolder}/scripts/debug.sh'",
            "type": "shell"
        }
    ]
}